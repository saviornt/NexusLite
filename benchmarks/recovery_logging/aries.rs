// Create an ARIES-style WAL with periodic checkpoints for benchmarking.
// Results should be logged and appended to `benchmarks/results/recovery_logging_{date}.csv`
// And then log the resulting benchmark